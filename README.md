# G3 Tech organization standards

This repository provides the public G3 Tech organization profile and default community-health files for repositories that do not define their own.

Repository-specific instructions take precedence when a project needs a different process. Shared engineering workflows will be versioned before consumers adopt them so changes cannot silently alter an existing release pipeline.
